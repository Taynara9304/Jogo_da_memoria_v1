import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: 170,
        height: 170,
        backgroundColor: '#bfbae6',
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 10,
    },
    image: {
        width: 150,
        height: 150,
        borderRadius: 10,
    }
});

export default styles;